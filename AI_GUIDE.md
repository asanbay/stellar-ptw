# 🤖 Внутренний AI модуль Stellar PTW

## 🎯 Обзор

Полностью автономная AI система, работающая локально без внешних API. Все данные остаются внутри системы.

## ✨ Возможности

### 1. 🎯 Анализ рисков (Risk Analyzer)

Автоматический анализ опасности работ на основе описания.

**Функции:**
- Классификация уровня риска (низкий, средний, высокий, критический)
- Определение потенциальных опасностей
- Рекомендации по СИЗ (средства индивидуальной защиты)
- Необходимые меры безопасности
- Уровень уверенности (confidence score)

**Пример использования:**

```typescript
import { useRiskAnalysis } from '@/hooks/use-ai'

function MyComponent() {
  const { riskAssessment, isAnalyzing } = useRiskAnalysis(
    "Сварочные работы на высоте 15 метров",
    'ru'
  )

  if (riskAssessment) {
    console.log('Уровень риска:', riskAssessment.level) // 'high'
    console.log('Score:', riskAssessment.score) // 80
    console.log('Опасности:', riskAssessment.hazards)
    // ['Падение с высоты', 'Пожар', 'Ожоги']
    console.log('Требуемые СИЗ:', riskAssessment.requiredPPE)
    // ['Страховочный пояс', 'Сварочная маска', 'Каска']
  }
}
```

**Поддерживаемые типы работ:**
- ✅ Работы на высоте
- ✅ Электромонтажные работы
- ✅ Сварочные работы
- ✅ Работы в замкнутых пространствах
- ✅ Химические работы
- ✅ Грузоподъемные работы
- ✅ Земляные работы

---

### 2. 💡 Умные подсказки (Smart Suggestions)

Самообучающаяся система на основе истории выполненных работ.

**Функции:**
- Автодополнение описания работ
- Предложение продолжительности
- Рекомендации по СИЗ и мерам безопасности
- Поиск похожих работ
- Популярные шаблоны

**Примеры:**

```typescript
import { useAutoComplete, useSmartSuggestions } from '@/hooks/use-ai'

// Автодополнение
function PermitForm() {
  const autoComplete = useAutoComplete(workDescription, 'ru')
  
  if (autoComplete && autoComplete.confidence > 0.6) {
    // Применяем подсказки автоматически
    setDuration(autoComplete.duration)
    setRequiredPPE(autoComplete.requiredPPE)
    setSafetyMeasures(autoComplete.safetyMeasures)
  }
}

// Подсказки при вводе
function DescriptionInput() {
  const suggestions = useSmartSuggestions(partialText, 'description')
  
  return (
    <div>
      {suggestions.map((s) => (
        <button onClick={() => setText(s.value)}>
          {s.value} (использовано {s.reason})
        </button>
      ))}
    </div>
  )
}
```

**Обучение системы:**

```typescript
import { useAILearning } from '@/hooks/use-ai'

function CompletePermit() {
  const { learn } = useAILearning()
  
  const handleComplete = () => {
    // Система учится на каждой выполненной работе
    learn({
      description: "Монтаж кабельных лотков",
      duration: 180, // минут
      requiredPPE: ["Каска", "Перчатки"],
      safetyMeasures: ["Ограждение зоны"],
      workers: 3
    })
  }
}
```

---

### 3. 👥 Интеллектуальный подбор персонала (Personnel Matcher)

AI для оптимального подбора команды.

**Функции:**
- Подбор подходящих сотрудников по роли
- Формирование оптимальной команды
- Учет квалификации и опыта
- Проверка соответствия требованиям
- Поиск замены сотрудника

**Примеры:**

```typescript
import { usePersonnelRecommendations, useTeamBuilder } from '@/hooks/use-ai'

// Подбор персонала
function PersonnelSelector() {
  const { recommendations, isLoading } = usePersonnelRecommendations(
    "Электромонтажные работы",
    allPersonnel,
    { role: 'worker', language: 'ru' }
  )
  
  return (
    <div>
      {recommendations.map((rec) => (
        <div>
          <p>{rec.person.name} - Score: {rec.score}</p>
          <ul>
            {rec.reasons.map((r) => <li>✓ {r}</li>)}
          </ul>
        </div>
      ))}
    </div>
  )
}

// Формирование команды
function TeamBuilder() {
  const { teamSuggestion, isBuilding, buildTeam } = useTeamBuilder(
    workDescription,
    allPersonnel,
    5, // размер команды
    'ru'
  )
  
  return (
    <div>
      <button onClick={buildTeam}>Сформировать команду</button>
      {teamSuggestion && (
        <div>
          <p>Score команды: {teamSuggestion.totalScore}</p>
          <p>Покрытие ролей: {teamSuggestion.coverage.roles.join(', ')}</p>
          {teamSuggestion.warnings.map((w) => <p>⚠️ {w}</p>)}
        </div>
      )}
    </div>
  )
}
```

---

### 4. 🔍 Детектор аномалий (Anomaly Detector)

Умная проверка качества данных.

**Функции:**
- Валидация нарядов-допусков
- Проверка корректности дат
- Выявление несоответствий
- Оценка качества данных (0-100)
- Рекомендации по улучшению

**Пример:**

```typescript
import { useDataValidation } from '@/hooks/use-ai'

function PermitValidation() {
  const { validation, isValidating } = useDataValidation(
    permitData,
    'permit',
    'ru'
  )
  
  if (validation) {
    return (
      <div>
        <p>Качество данных: {validation.score}/100</p>
        
        {validation.anomalies.map((anomaly) => (
          <div className={`severity-${anomaly.severity}`}>
            <p>⚠️ {anomaly.reason}</p>
            {anomaly.suggestion && <p>💡 {anomaly.suggestion}</p>}
          </div>
        ))}
        
        {validation.recommendations.map((r) => (
          <p>{r}</p>
        ))}
      </div>
    )
  }
}
```

---

### 5. 🎯 Комплексный анализ (Comprehensive Analysis)

Объединяет все AI модули для полного анализа.

```typescript
import { useComprehensiveAnalysis } from '@/hooks/use-ai'

function PermitAnalysis() {
  const { analysis, isAnalyzing } = useComprehensiveAnalysis(
    permitData,
    {
      language: 'ru',
      allPersonnel: personnel,
      teamSize: 5,
    }
  )
  
  if (analysis) {
    return (
      <div>
        {/* Риски */}
        <RiskPanel risk={analysis.riskAssessment} />
        
        {/* Качество данных */}
        <QualityPanel quality={analysis.dataQuality} />
        
        {/* Подсказки */}
        <SuggestionsPanel suggestions={analysis.suggestions} />
        
        {/* Рекомендации по персоналу */}
        <PersonnelPanel recommendations={analysis.personnelRecommendations} />
        
        {/* Команда */}
        <TeamPanel team={analysis.teamSuggestion} />
      </div>
    )
  }
}
```

---

## 📊 Примеры работы

### Пример 1: Анализ опасной работы

**Вход:**
```
Описание: "Сварка металлоконструкций на высоте 20 метров"
```

**Выход:**
```javascript
{
  level: 'critical',
  score: 90,
  hazards: [
    'Падение с высоты',
    'Пожар',
    'Взрыв',
    'Ожоги',
    'Поражение глаз'
  ],
  requiredPPE: [
    'Страховочный пояс',
    'Сварочная маска',
    'Огнестойкая одежда',
    'Каска'
  ],
  safetyMeasures: [
    'Ограждение зоны работ',
    'Огнетушитель',
    'Проверка страховочного оборудования',
    'Наблюдатель'
  ],
  recommendations: [
    '⚠️ КРИТИЧЕСКИЙ УРОВЕНЬ РИСКА! Требуется особое внимание',
    'Обязательна разработка плана производства работ',
    'Необходимо согласование с службой ОТ и ПБ'
  ],
  confidence: 0.95
}
```

### Пример 2: Умные подсказки

**Сценарий:** Пользователь начинает вводить "Монтаж..."

**AI предлагает:**
```javascript
[
  {
    value: "Монтаж кабельных лотков в помещении электрощитовой",
    confidence: 0.85,
    reason: "Использовано 12 раз"
  },
  {
    value: "Монтаж вентиляционного оборудования",
    confidence: 0.72,
    reason: "Использовано 8 раз"
  }
]
```

**После выбора AI автоматически заполняет:**
- Продолжительность: 180 минут
- СИЗ: ["Каска", "Перчатки", "Диэлектрическая обувь"]
- Меры: ["Отключение питания", "Ограждение зоны"]
- Рабочих: 3

---

## 🎓 Обучение системы

AI учится автоматически на каждой выполненной работе:

```typescript
// В компоненте завершения наряда
const handleCompletePermit = async (permit) => {
  await completePermit(permit)
  
  // AI запоминает паттерн
  stellarAI.learnFromWork({
    description: permit.work_description,
    duration: permit.actual_duration,
    requiredPPE: permit.required_ppe,
    safetyMeasures: permit.safety_measures,
    workers: permit.workers.length
  })
}
```

**Статистика обучения:**

```typescript
const { stats } = useAIStats()

console.log('Шаблонов в базе:', stats.totalTemplates)
console.log('Всего использований:', stats.totalUsage)
console.log('Популярные работы:', stats.popularTemplates)
```

---

## 💾 Экспорт/Импорт обученных данных

```typescript
import { useAILearning } from '@/hooks/use-ai'

function AISettings() {
  const { exportLearning, importLearning } = useAILearning()
  
  // Экспорт
  const handleExport = () => {
    const data = exportLearning()
    downloadJSON(data, 'ai-learning-data.json')
  }
  
  // Импорт
  const handleImport = (file) => {
    const data = JSON.parse(file)
    importLearning(data)
  }
}
```

---

## ⚡ Производительность

- ✅ **Мгновенный анализ** - все работает локально
- ✅ **Нет задержек сети** - не требует интернета
- ✅ **Приватность** - данные не покидают систему
- ✅ **Бесплатно** - без подписок и API ключей
- ✅ **Офлайн** - работает без подключения

**Типичное время отклика:**
- Анализ рисков: ~10-50мс
- Подсказки: ~5-20мс
- Подбор персонала: ~20-100мс
- Валидация: ~10-30мс

---

## 🔧 Настройка

### Debounce для real-time анализа

```typescript
// По умолчанию 500мс
const { riskAssessment } = useRiskAnalysis(text, 'ru', 300)

// Можно настроить под себя
```

### Пороги уверенности

```typescript
// Применять подсказки только при уверенности >70%
if (autoComplete.confidence > 0.7) {
  applyS uggestions(autoComplete)
}
```

---

## 🌍 Мультиязычность

Все AI модули поддерживают 3 языка:
- 🇷🇺 Русский
- 🇹🇷 Турецкий
- 🇬🇧 Английский

```typescript
const analysis = stellarAI.quickRiskCheck(text, 'tr')
// Все сообщения будут на турецком
```

---

## 📈 Будущие улучшения

### Планируется:
1. **ML модель** - использование TensorFlow.js для более точного анализа
2. **NLP** - улучшенная обработка естественного языка
3. **Временные ряды** - прогнозирование на основе истории
4. **Кластеризация** - автоматическая группировка похожих работ
5. **Sentiment Analysis** - анализ тональности комментариев

---

## 🎯 Кейсы использования

### 1. Быстрое создание наряда
```
Пользователь вводит: "Замена светильников"
→ AI предлагает шаблон из истории
→ Автозаполнение СИЗ, мер, времени
→ Подбор подходящих электриков
→ Готовый наряд за 30 секунд!
```

### 2. Проверка безопасности
```
Описание: "Работы на крыше"
→ AI: ⚠️ ВЫСОКИЙ РИСК!
→ Требуется: страховочный пояс, наблюдатель
→ Автоматическое добавление мер безопасности
```

### 3. Оптимизация команды
```
Работа: "Монтаж оборудования 500кг"
→ AI подбирает: руководителя + 2 монтажников
→ Проверяет квалификацию
→ Предупреждает о нехватке персонала
```

---

Создано: 28 ноября 2025  
Версия: 1.0.0  
Лицензия: MIT
