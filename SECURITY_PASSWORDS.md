# 🔐 Безопасность паролей - Инструкция

## ✅ Что сделано:

### 1. Пароли перенесены в переменные окружения
- ❌ Раньше: пароли `'123'` и `'superadmin'` были прямо в коде
- ✅ Теперь: пароли в `.env` файле (не попадает в Git)

### 2. Файлы созданы:
- `.env` - локальные переменные (НЕ коммитится)
- `.env.example` - шаблон для других разработчиков

---

## 📋 Следующие шаги:

### ШАГ 1: Сделать репозиторий приватным на GitHub

1. **Откройте** GitHub → stellar-ptw
2. **Справа** найдите кнопку "Visibility" 
3. **Выберите** "Only you" (Private)
4. **Нажмите** зеленую кнопку "Publish"

✅ Теперь код виден только вам!

---

### ШАГ 2: Добавить пароли в Vercel

Vercel не видит ваш `.env` файл. Нужно добавить переменные вручную:

1. **Откройте** https://vercel.com/dashboard
2. **Выберите** проект "stellar-ptw"
3. **Settings** → **Environment Variables**
4. **Добавьте** две переменные:

```
Name: VITE_ADMIN_PASSWORD
Value: [ваш новый безопасный пароль для админа]

Name: VITE_SUPER_ADMIN_PASSWORD  
Value: [ваш новый безопасный пароль для супер-админа]
```

5. **Примените** к: Production, Preview, Development
6. **Save**

---

### ШАГ 3: Redeploy в Vercel

После добавления переменных:

1. **Deployments** → последний деплой
2. **⋯ (три точки)** → **Redeploy**
3. Подождите 30-60 секунд

✅ Сайт перезапустится с новыми паролями!

---

## 🔒 Рекомендации по паролям:

### Хорошие пароли:
- `Stellar2025!PTW` (админ)
- `SuperAdmin#2025@PTW` (супер-админ)

### Плохие пароли:
- ❌ `123` - слишком простой
- ❌ `admin` - легко угадать
- ❌ `password` - стандартный

---

## 🛡️ Что теперь защищено:

| Уровень | Защита |
|---------|--------|
| GitHub код | ✅ Private репозиторий (только вы) |
| Локальная разработка | ✅ Пароли в `.env` (не коммитится) |
| Production сайт | ✅ Пароли в Vercel Environment Variables |
| Supabase URL/Key | ✅ Публичные ключи (безопасны по дизайну) |

---

## ❓ FAQ

**Q: Можно ли коммитить .env?**
❌ НЕТ! `.env` в `.gitignore` - Git его игнорирует.

**Q: Что если забуду пароль?**
✅ Посмотрите в `.env` локально или в Vercel Settings.

**Q: Нужно ли менять Supabase ключи?**
✅ Нет, `SUPABASE_ANON_KEY` безопасен для публичного использования (так задумано).

**Q: Как поменять пароли?**
1. Измените в `.env` локально
2. Измените в Vercel Environment Variables  
3. Redeploy в Vercel

---

## 🚀 Проверка:

После выполнения всех шагов проверьте:

```bash
# 1. Локально - .env не коммитится
git status
# Не должно быть ".env" в списке

# 2. GitHub - репозиторий приватный
# Откройте github.com/asanbay/stellar-ptw
# Должен быть значок 🔒 Private

# 3. Vercel - переменные добавлены
# Settings → Environment Variables
# Должны быть VITE_ADMIN_PASSWORD и VITE_SUPER_ADMIN_PASSWORD

# 4. Сайт - проверьте вход
# Откройте stellar-ptw.me
# Войдите с новыми паролями
```

---

**Готово!** Теперь ваши пароли в безопасности! 🎉
