# 🧪 Stellar PTW — Test Plan

## 🎯 Цели
- Подтвердить, что прод-версия работает в браузере без ошибок (Chrome/Edge).
- Проверить все ключевые сценарии: входы, импорт/экспорт, CRUD, Supabase синхронизация.
- Валидировать мультиязычность и адаптивность.

## 🧱 Тестовое окружение
- **Build**: `npm run build && npm run preview` или Netlify preview.
- **Браузеры**: Chrome 129+, Edge 129+.
- **Данные**: демо-пакет + Supabase проект (по желанию).
- **Аккаунты**: клиентские пароли `123` (admin) и `admin123` (super_admin).

## ✅ Чек-лист сценариев

| ID | Сценарий | Действия | Ожидаемый результат |
|----|----------|----------|---------------------|
| F01 | User mode | Открыть приложение, не логиниться | Все вкладки доступны для чтения, нет ошибок в консоли |
| F02 | Admin login | Нажать «Админ» → `123` | Появляются кнопки добавления, доступ к импортам, toast «Вошли как админ» |
| F03 | Super Admin | Нажать «Админ» → `admin123` | Появляется вкладка `Super Admin Dashboard`, toast «Logged in as Super Admin» |
| F04 | Import preview | Скачать шаблон, заполнить 2 строки, импортировать | Предпросмотр показывает записи, роли определены автоматически, успех в toast |
| F05 | Import commit | Нажать «Импортировать» | Данные появляются в списке персонала / Supabase, консоль без ошибок |
| F06 | Export CSV | Вкладка Personnel → «Экспорт» | Скачивается CSV с UTF‑8 BOM, открывается в Excel без кракозябр |
| F07 | PTW CRUD | Создать/редактировать/удалить PTW | Изменения видны сразу, без ошибок |
| F08 | Info board | Добавить объявление, переключиться в user mode | Объявление отображается для всех |
| F09 | Language switch | Переключить RU/TR/EN | UI тексты меняются, выбранная локаль сохраняется |
| F10 | Theme switch | Переключить тему, перезагрузить | Тема сохраняется, нет визуальных артефактов |
| F11 | Supabase toggle | С `.env` → проверить сетевые запросы | В Network видно вызовы Supabase; при удалении `.env` данные читаются из localStorage |
| F12 | Windows CSV | Пройти инструкцию `WINDOWS_CSV_FIX.md` | CSV открывается в Excel с разделителем `;` / `,` |
| N01 | Offline fallback | Отсутствие `.env`, очистка storage | Приложение работает в демо-режиме, без Supabase ошибок |
| R01 | Error boundary | В dev моде временно бросить ошибку | Появляется компонент `ErrorFallback` |

## 🔄 Регрессии, на которые обратить внимание
- Импорт крупных CSV (>500 строк) — прогонить на staging.
- Автодетект ролей (рус/тур) — проверить крайние кейсы.
- Производительность dashboard (chunk предупреждение) — мониторить в lighthouse.

## 📋 Отчёт после тестов
1. Заполнить таблицу `Test Run` (дата, окружение, статус).
2. Для провалов приложить скрин/лог, открыть issue.
3. Приложить список версий (commit SHA, build URL).

Готово! Этот план можно передать заказчику или использовать как чек-лист перед релизом.
