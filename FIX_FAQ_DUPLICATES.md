# Исправление дублирования FAQ

## Проблема
FAQ записи дублировались при каждом добавлении, из-за отсутствия проверки на существующие вопросы.

## Что было исправлено

### ✅ 1. Проверка на дубликаты в handleAddFAQ

**До:**
```typescript
const handleAddFAQ = async (faqData: Partial<FAQItem>) => {
  // Просто добавлялось без проверок
  const created = await faqStore.create(buildFAQInsert(faqData))
  setRemoteFaqs([...current, mapped])
}
```

**После:**
```typescript
const handleAddFAQ = async (faqData: Partial<FAQItem>) => {
  // Проверка на дубликаты
  const isDuplicate = allFaqs.some(f => 
    f.question[language] === faqData.question?.[language]
  )
  
  if (isDuplicate) {
    toast.warning('Этот вопрос уже существует')
    return // Не добавляем дубликат
  }
  
  // Добавляем только уникальные вопросы
  const created = await faqStore.create(buildFAQInsert(faqData))
}
```

### ✅ 2. Улучшено логирование

Теперь в консоли видно:
```
📝 handleAddFAQ вызван: { question, answer, category }
⚠️ FAQ уже существует, пропускаем
✅ FAQ добавлен в Supabase
```

### ✅ 3. Работает для обоих режимов

- **Supabase:** Проверка перед отправкой в БД
- **localStorage:** Проверка перед сохранением локально

---

## Как удалить существующие дубликаты

### Вариант 1: Через Supabase Dashboard

1. Откройте Supabase Dashboard
2. Перейдите в Table Editor → `faq`
3. Найдите дубликаты (сортируйте по `question`)
4. Удалите лишние записи вручную

### Вариант 2: Через SQL

```sql
-- Удаляем дубликаты, оставляя только первую запись
DELETE FROM faq a
USING faq b
WHERE a.id > b.id 
  AND a.question = b.question;
```

### Вариант 3: Через UI приложения

1. Войдите как администратор
2. Перейдите на вкладку FAQ
3. Удалите дубликаты вручную через кнопку "Удалить"

---

## Проверка исправления

### 1️⃣ Попытка добавить дубликат

1. Войдите как администратор
2. Откройте вкладку FAQ
3. Нажмите "Добавить вопрос"
4. Введите вопрос, который уже существует
5. Нажмите "Сохранить"
6. **Ожидается:** Предупреждение "Этот вопрос уже существует"

### 2️⃣ Проверка логов в консоли

Откройте консоль браузера (F12):
```
📝 handleAddFAQ вызван: { ... }
⚠️ FAQ уже существует, пропускаем
```

---

## Дополнительные улучшения

### Проверка по всем языкам (опционально)

Если нужно проверять дубликаты по всем языкам сразу:

```typescript
const isDuplicate = allFaqs.some(f => 
  f.question.ru === faqData.question?.ru ||
  f.question.tr === faqData.question?.tr ||
  f.question.en === faqData.question?.en
)
```

### Предупреждение при редактировании

Можно добавить проверку и в `handleEditFAQ`:

```typescript
const handleEditFAQ = async (id: string, faqData: Partial<FAQItem>) => {
  // Проверяем только другие FAQ (не текущий)
  const isDuplicate = allFaqs.some(f => 
    f.id !== id && f.question[language] === faqData.question?.[language]
  )
  
  if (isDuplicate) {
    toast.warning('Такой вопрос уже существует')
    return
  }
  
  // ... остальная логика
}
```

---

## Тестирование

- [x] Добавление нового уникального вопроса - работает ✅
- [x] Попытка добавить дубликат - блокируется ✅
- [x] Редактирование существующего - работает ✅
- [x] Удаление - работает ✅
- [x] Логирование в консоль - работает ✅

---

**Статус:** ✅ Исправлено  
**Дата:** 2 декабря 2025  
**Влияние:** Защита от дублирования в будущем
