# 🚀 Настройка Supabase для Stellar PTW

## Шаг 1: Создание проекта Supabase

1. Зайдите на [supabase.com](https://supabase.com)
2. Нажмите **"New Project"**
3. Заполните:
   - **Name**: `stellar-ptw`
   - **Database Password**: (надежный пароль - сохраните его!)
   - **Region**: выберите ближайший регион
4. Нажмите **"Create new project"**
5. Дождитесь создания (1-2 минуты)

## Шаг 2: Выполнение SQL миграции

1. В Supabase перейдите в **SQL Editor** (боковое меню)
2. Нажмите **"New query"**
3. Откройте файл `supabase/migrations/001_initial_schema.sql` в этом проекте
4. Скопируйте **весь SQL код**
5. Вставьте в Supabase SQL Editor
6. Нажмите **"Run"** (или Ctrl+Enter)
7. Дождитесь сообщения: `Success. No rows returned`

✅ База данных создана!

## Шаг 3: Получение API ключей

1. В Supabase перейдите в **Settings** → **API** (боковое меню)
2. Найдите секцию **"Project API keys"**
3. Скопируйте:
   - **Project URL** (например: `https://xxxyyzzz.supabase.co`)
   - **anon public** ключ (длинная строка)

## Шаг 4: Настройка .env файла

1. Откройте файл `.env` в корне проекта
2. Вставьте ваши данные:

\`\`\`env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-anon-ключ-здесь
VITE_APP_NAME=Stellar PTW
VITE_APP_VERSION=1.0.0
\`\`\`

3. Сохраните файл

## Шаг 5: Режимы доступа в приложении

Аутентификация по email/Supabase пока не используется. Доступ регулируется внутри приложения:

- `123` — стандартный админ (редактирование данных)
- `admin123` — Super Admin Dashboard (дополнительные панели, аналитика)

Эти пароли можно поменять в `src/components/LoginDialog.tsx`.

## Шаг 6: Тестирование подключения

Запустите приложение:

\`\`\`bash
npm run dev
\`\`\`

Откройте браузер и проверьте консоль. Если видите ошибки про Supabase - проверьте `.env` файл и перезапустите `npm run dev`.

## Шаг 7: Проверка UI

1. Откройте приложение
2. Нажмите **"Админ"** в шапке
3. Введите пароль `123` (админ) или `admin123` (супер-админ)
4. Убедитесь, что данные загружаются из Supabase (проверьте Network/Console)

## 🔐 Безопасность

### Важно:
- ✅ Файл `.env` уже добавлен в `.gitignore`
- ❌ **НИКОГДА** не коммитьте `.env` в Git
- ❌ Не публикуйте скриншоты с ключами
- ✅ Для production используйте environment variables в Netlify/Vercel

### Netlify/Vercel Environment Variables:

Добавьте в настройках деплоя:
- `VITE_SUPABASE_URL` = ваш URL
- `VITE_SUPABASE_ANON_KEY` = ваш ключ

## 📊 Проверка работы Row Level Security (RLS)

После настройки проверьте:

1. **Как обычный пользователь:**
   - ✅ Может просматривать сотрудников, отделы, наряды
   - ❌ Не может добавлять/редактировать/удалять

2. **Как админ:**
   - ✅ Может все просматривать
   - ✅ Может добавлять/редактировать/удалять

Если что-то не работает - проверьте RLS политики в Supabase.

## 🎉 Готово!

Теперь ваше приложение работает с реальной базой данных Supabase!

### Полезные ссылки:

- 📊 **Supabase Dashboard**: `https://app.supabase.com/project/ваш-проект`
- 📖 **Table Editor**: Supabase → Table Editor (можно редактировать данные)
- 🔐 **Authentication**: Supabase → Authentication (управление пользователями)
- 📈 **SQL Editor**: Supabase → SQL Editor (запросы к БД)
- 🔍 **API Docs**: Supabase → API Docs (автогенерируемая документация)

## ❓ Частые проблемы

### "Failed to fetch" ошибка
- Проверьте правильность VITE_SUPABASE_URL
- Убедитесь, что проект Supabase запущен

### "Invalid API key"
- Проверьте VITE_SUPABASE_ANON_KEY
- Убедитесь, что копировали **anon** ключ, а не **service_role**

### "Row Level Security policy violation"
- Убедитесь, что вы вошли в систему
- Проверьте, что роль пользователя правильно установлена

### Данные не сохраняются
- Откройте консоль браузера (F12)
- Проверьте есть ли ошибки
- Убедитесь, что .env файл загружен (перезапустите `npm run dev`)

## 🔄 Миграция существующих данных

Если у вас уже есть данные в localStorage (из демо):

\`\`\`typescript
// TODO: Создать скрипт миграции из localStorage в Supabase
// Это можно сделать через консоль браузера или отдельный скрипт
\`\`\`

---

**Нужна помощь?** Проверьте [Supabase Documentation](https://supabase.com/docs)
